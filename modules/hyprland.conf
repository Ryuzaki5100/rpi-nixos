# Hyprland starter config for the Raspberry Pi 5 desktop (seeded on first boot).
# Note: Noctalia is the shell/bar; Hyprland is the compositor underneath it.

monitor = , preferred, auto, 1

# Execute Noctalia here as a fallback launch (the NixOS module normally manages
# it as a systemd user service bound to graphical-session.target).
exec-once = noctalia

# Rules
general {
  gaps_in = 6
  gaps_out = 12
  border_size = 2
  col.active_border = rgba(89caebee) rgba(81a1f1ee) 45deg
  col.inactive_border = rgba(2e3440cc)
  layout = master
}

decoration {
  rounding = 8
  blur {
    enabled = true
    size = 5
    passes = 2
  }
  shadow {
    enabled = false
  }
}

animations {
  enabled = yes
  bezier = easeOutCubic, 0.18, 0.67, 0.36, 1.0
  animation = windows, 1, 5, easeOutCubic
  animation = fade, 1, 4, easeOutCubic
  animation = workspaces, 1, 5, easeOutCubic
}

input {
  kb_layout = us
  follow_mouse = 1
  touchpad {
    natural_scroll = false
  }
}

misc {
  disable_hyprland_logo = true
  vfr = true
}

# Bindings
bindsym = SUPER, Return, exec, foot
bindsym = SUPER, Q, killactive
bindsym = SUPER, M, exit
bindsym = SUPER, Space, exec, noctalia msg panel-toggle launcher
bindsym = SUPER, V, exec, noctalia msg panel-toggle clipboard
bindsym = SUPER, N, exec, noctalia msg panel-toggle control-center
bindsym = SUPER, L, exec, noctalia msg session lock

# Workspace switching
bindsym = SUPER, 1, workspace, 1
bindsym = SUPER, 2, workspace, 2
bindsym = SUPER, 3, workspace, 3
bindsym = SUPER, 4, workspace, 4

bindsym = SUPER SHIFT, 1, movetoworkspace, 1
bindsym = SUPER SHIFT, 2, movetoworkspace, 2
bindsym = SUPER SHIFT, 3, movetoworkspace, 3
bindsym = SUPER SHIFT, 4, movetoworkspace, 4

bindsym = SUPER, F, fullscreen
bindsym = SUPER, C, exec, wl-copy --clear